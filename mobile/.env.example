# Copy to .env and fill in from the web app's .env.local.
# These are the PUBLIC url + anon key (safe to embed in the app binary);
# never put the service-role key or any secret key here.
EXPO_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
